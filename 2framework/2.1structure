tf 采用数据流图计算 （data flow graphs）
创建数据流图，再将数据（张量tensor)放在数据流图中计算

nodes 表示数学操作
edges 表示节点间相互联系的多维数据数组（tensor）
训练时 tensor从一个node flow到另一个node

tensor：
    张量有多种. 零阶张量为 纯量或标量 (scalar) 也就是一个数值. 比如 [1]
    一阶张量为 向量 (vector), 比如 一维的 [1, 2, 3]
    二阶张量为 矩阵 (matrix), 比如 二维的 [[1, 2, 3],[4, 5, 6],[7, 8, 9]]
    以此类推, 还有 三阶 三维的 …
